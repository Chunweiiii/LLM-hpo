# 決策紀錄

供論文 Method 章節引用,誠實記錄每個閾值/演算法是「從資料算出來的」還是
「憑經驗設定的」。

## 已確定

| 項目 | 數值 | 依據 |
|---|---|---|
| 實驗室顯卡 | NVIDIA RTX PRO 4000 Blackwell,24GB GDDR7,672 GB/s 頻寬(原本筆電 8GB 的 3 倍) | 2026-09 確認 |
| 實驗室機器系統 RAM | 16GB | 2026-09 確認。相對吃緊,已據此把 YOLO 訓練的 `workers` 保守鎖定為 4(見下方) |
| 雪糕實際會用哪台機器跑 | 實驗室的機器(跟這台同款 GPU,NVIDIA RTX PRO 4000 Blackwell 24GB) | 2026-09 確認。他自己的筆電是 RTX 5060 Laptop(8GB VRAM,跟使用者原本的筆電同級),但正式要跑這個專案的實驗是用實驗室機器,硬體規格跟這台完全一樣,所以 batch_size=48、baseline mAP/雜訊門檻、D4/D5 閾值都可以直接沿用,不需要重測。如果他之後改用自己的筆電額外驗證/延伸,才需要照 8GB VRAM 的情況重新跑 `scripts/find_batch_size.py`,那種情況下也要注意 Qwen3-14B(9.3GB)在 8GB VRAM 上可能塞不下 |
| 診斷架構 | 兩層(Facts + 5 Diagnostic Flags),不做第三層參數關聯提示 | 避免研究者先驗混入 LLM 判斷,見交接文件第四節 |
| D1 起始點判定門檻 | 0.7(× 總epoch數) | 憑經驗設定,已誠實記錄;意義是「浪費超過30%訓練預算才算嚴重plateau」 |
| 關鍵點抽樣的轉折點演算法 | 簡單斜率變化(不用 change point detection / PELT) | 時程考量,PELT需額外套件與調參,效益不值得投入,可列為 Future Work |
| Domain Knowledge 段落去留 | 不衝突,予以保留 | `prompts/program.md` 裡的 Model Architecture / Training Stack 是客觀事實描述,不是「診斷類型→該調哪個參數」的提示性內容,跟「不給參數關聯提示」的決策不衝突 |
| 本機/開源 LLM 選型 | Qwen3-14B(取代原本的 Qwen2.5-Coder-7B) | 2026-09 確認新機器 24GB VRAM 後重新評估。原本選 Coder 系列是誤植:這個任務是讀診斷報告推理出超參數建議、輸出 JSON,屬於通用推理任務,不是程式碼生成任務,Coder 系列並非最佳選擇。改用 Qwen3 通用推理模型(內建 thinking 模式),4-way 比較也因此變成「通用推理模型」之間的橫向比較,邏輯更一致。14B(9.3GB)在 24GB VRAM 上有充足餘裕,thinking trace 額外存起來(`_thinking` 欄位)作為質化分析素材 |
| YOLO 訓練 `workers` | 4(不用 Ultralytics 預設值) | 實驗室機器系統 RAM 僅 16GB,Windows 下每個 worker 是獨立 process,設太高有機會在訓練時把記憶體壓滿,保守鎖定為 4,見 `src/train_runner.py` |
| 資料集路徑 | `./datasets`(標準 Roboflow 匯出結構:`test/train/valid` + `data.yaml` 直接在這層底下,沒有額外的 `traffic_light_v2` 子資料夾) | 資料集 repo(GitHub, `llm-hpo-yolov8-v2`)本身內部帶一層同名路徑,clone/手動整理過程中經過幾次調整,最終版本確認是直接放在 `datasets/` 底下。`scripts/find_batch_size.py` 改成從 settings 讀路徑,不再寫死,避免兩處路徑不一致 |
| batch_size | 48(實測值 49,往下取整) | 2026-09 在 RTX PRO 4000 Blackwell 上用 `scripts/find_batch_size.py`(Ultralytics AutoBatch,imgsz=640,目標 60% GPU 記憶體使用率)實測得出:`Using batch-size 49 for CUDA:0 14.04G/23.89G (59%)`。往下取整為 48(16 的倍數,效率損失可忽略,留一點 VRAM 餘裕)。之後所有實驗固定用這個值,不再自動判定 |
| D1 平台期起點 / 關鍵點抽樣轉折點,兩者演算法定義 | 見 `src/summarize_log.py` `_find_plateau_start()` 與 `find_key_points()` | 兩者都是簡單斜率變化,但目的不同,定義也不同:D1 用**絕對雜訊門檻**(平滑後逐輪變化量持續低於 `NOISE_THRESHOLD` 的起始點);關鍵點抽樣的「轉折點」用**相對早期斜率的比例**(平滑後斜率掉到前25%訓練輪次平均斜率的10%以下,且之後70%以上時間維持在30%以下,判定為成長明顯趨緩)。兩個都不用 change point detection / PELT |
| per-class 統計來源 | 訓練完成後,對 best.pt 額外跑一次 `model.val()` | `results.csv` 只有整體(all)的逐epoch指標,沒有 per-class 資料,D5 診斷跟 Facts 層的 per-class AP/P/R/n 都需要額外驗證一次才能取得,見 `src/train_runner.py` 的 `compute_class_stats()`。這會讓每輪訓練多一次驗證時間,但是 D5 診斷跟論文表格都需要這份資料,無法省略 |
| Baseline mAP50-95(第一版,舊機器,已過時) | ~~0.6585 ± 0.0038(3-seed)~~ | 用舊機器(RTX 5060 Laptop)、舊的 4 參數 `INITIAL_HYPERPARAMETERS`(含 `lr0=0.001429`)測的,換新機器、擴充到 5 個可調參數後已不能用 |
| 雜訊門檻(第一版,已過時) | ~~0.0076~~ | 同上 |
| Baseline mAP50-95(第二版,SEARCH_SPACE 中點,已過時) | ~~0.6606 ± 0.0060(3-seed)~~ | 用 `src/baseline_search.py` `SEARCH_SPACE` 的中點當起始超參數測的(`lr0=0.001`/`weight_decay≈0.000316`/`mosaic=0.5`/`hsv_v=0.5`/`box=8.5`)。指導教授指出這個數字跟 HPO 找到的最佳值(約0.67)差距太小,會有天花板效應,難以展示優化方法的價值,故棄用,改用下面的「新手起始點」版 |
| D4/D5 閾值(第二版,已過時) | ~~D4=0.3155 / D5=0.1991~~ | 同上,用第二版 baseline 算的 |
| per-class 統計的兩個 bug | 都已修正並實測確認,這個修正不受超參數組合影響,不用重做 | (1) 一開始跑 3-seed baseline 時 `train_runner.py` 還沒有 `compute_class_stats()`,導致結果缺 `class_stats` 欄位。(2) 加上 `compute_class_stats()` 之後,`n`(instance數)一路是 `None`,原因是程式碼把 `nt_per_class` 這個屬性寫在 `metrics.box` 底下,但實測(`dir(metrics)` / `dir(metrics.box)`)發現它其實掛在 `metrics` 底下,不是 `metrics.box`。兩者都用 `scripts/derive_d4_d5_thresholds.py` 內建的自動偵測補算邏輯修好,對每個 seed 已存在的 `best.pt` 補跑一次 `model.val()`(不重新訓練),補齊後寫回同一份 json。修好後三類別 n 加總(109+125+48=282)與驗證集總 instance 數完全吻合,確認正確 |
| 起始超參數 `lr0=0.001429` 的來源問題 | 已發現並棄用 | 這個值是使用者在舊機器上跑傳統 HPO 找到的,拿到新機器直接沿用,沒有文件記錄來源、也沒有重新驗證是否仍然合適。使用者明確要求「全新設計」,不再沿用任何機器特定的舊數字 |
| 可調超參數組合 | 5 個:`lr0`、`weight_decay`、`mosaic`、`hsv_v`、`box`(2026-09 從原本 4 個擴充,曾考慮過完整 9 個) | 三個理由收斂到 5 個而非 9 個:(1) 預算限制——`hpo.num_rounds=10`,傳統方法(random search/TPE/CMA-ES)只有 10 輪搜尋機會,9 維空間下 10 個樣本幾乎等於沒搜過,會讓「LLM 是否優於傳統方法」的比較失去意義(維度災難);(2) 文獻證據強度——見下面「文獻:超參數重要性」,只有 `lr0`、`weight_decay`、`mosaic` 有直接證據支持影響力;(3) 與 D1-D5 診斷旗標的對應完整性——`lr0`/`weight_decay` 對應 D1/D2/D3,`box` 對應 D4(唯一補上的新參數),`mosaic`/`hsv_v` 對應 D5。捨棄的參數沒有對應任何一個診斷旗標,加進去等於讓 LLM 有機會在沒有診斷根據的情況下亂調參數,稀釋「診斷資訊是否真的有幫助」這個核心論點 |
| 文獻:超參數重要性 | 見下方引用列表 | `lr0` 是證據最一致的關鍵超參數:Wang, Nabney & Golbabaee(arXiv:2410.08920, 2024)訓練萬餘個 CNN 做重要性評估,學習率排名第二(僅次於架構層數);Gill & Rajendran(*Scientific Reports*, 2026)在 YOLO 上用 Adam optimizer 調 `lr0` 讓 VisDrone2019 的 mAP50-95 提升 13.5%、recall 提升 18.42%。`weight_decay`/`momentum` 的最佳值明確跟 optimizer 選擇相關(同篇:Adam 下最佳 weight_decay≈1.64e-5,SGD 下 weight_decay≈0.001,相差60倍),本專案用 AdamW 時要注意。`mosaic` 有 YOLOv4 論文(Bochkovskiy et al., arXiv:2004.10934, 2020)Table 4 的開關消融數字(AP 38.0%→39.1%)。`box`/`cls`/`hsv_v`/`warmup_epochs`/`lrf` 查無專門的重要性/消融研究,`hsv_v` 保留主要是任務本身(紅黃綠燈辨識)高度依賴顏色資訊的領域判斷,不是文獻直接支持 |
| `box` 搜尋範圍 (2.0, 15.0) | 憑工程判斷設定,非文獻實證 | 以 YOLOv8 官方預設值 7.5 為中心抓的合理區間(約 0.27~2 倍),誠實記錄:不像 `lr0`/`weight_decay` 有明確文獻依據 |
| **起始超參數(novice baseline,最終定案)** | Ultralytics 框架預設值:`lr0=0.01`/`weight_decay=0.0005`/`mosaic=1.0`/`hsv_v=0.4`/`box=7.5` | 取代 SEARCH_SPACE 中點版。指導教授指出中點版 baseline 太高、有天花板效應後,改成模擬「新手沒調過參數」的真實狀態:直接採用 Ultralytics 框架本身的預設超參數,不做任何研究者的主觀判斷,完全客觀、可重現,用 `python -c "from ultralytics.cfg import DEFAULT_CFG; print(DEFAULT_CFG)"` 在這台機器實測版本上查證過。注意:`lr0=0.01` 剛好卡在 `SEARCH_SPACE` 上限、`mosaic=1.0` 也是,這兩個參數在起始狀態下只能往下調,誠實記錄為已知限制,不特別處理。這組起始點用於 novice baseline、score-only 對照組、四個 LLM 方法,對所有方法公平中立 |
| **Baseline mAP50-95(novice,5-seed,最終定案)** | mean=0.6198、std=0.0110(seed=0,1,2,3,4)、noise_threshold=0.0219(=2×std) | 3-seed 時(0.6214±0.0095)發現 seed1 明顯偏低(0.61038 vs 其他兩個 ~0.627),用逐 epoch loss(train/val loss 全面偏高)跟 class_stats(落差集中在 yellow light,樣本數最少的類別)檢查過,確認是正常訓練變異,不是 bug、不是訓練中斷。但 n=3 對變異數估計太不穩定,單一極端值影響力過大,故擴充到 5-seed 稀釋這個影響(而非把 seed1 剔除或換掉,那樣是挑資料)。5-seed 結果:seed3=0.60568 也偏低,seed1+seed3 兩個都偏低,std 從 0.0095 上升到 0.0110、noise_threshold 從 0.0191 上升到 0.0219,說明這組起始超參數(`lr0`/`mosaic` 卡在 SEARCH_SPACE 邊界)本身訓練穩定性確實比中點版低,這點誠實記錄在論文限制段落。`weakest_class` 在 5 個 seed 全部一致判定為 yellow light,排序穩定 |
| **D4/D5 閾值(novice,5-seed,最終定案)** | D4=0.326124(mean 0.321306 + 1×std 0.004818)、D5=0.226932(mean 0.203701 + 1×std 0.023231) | 用 `scripts/derive_d4_d5_thresholds.py` 對 5 個 seed 的 `results.csv`(D4:最終 epoch 的 mAP50-mAP50-95 落差)與 `class_stats`(D5:最強類別-最弱類別 AP50-95 落差)重新算出。D4 閾值跟 3-seed 版(0.3261)幾乎沒變(0.3261→0.3261),D5 閾值從 3-seed 版的 0.2353 降到 0.2269(3-seed 的 D5 std 被高估) |
| **pandas 被 Windows Application Control Policy 封鎖(baseline 方法繞過)** | `src/train_runner.py` 的 `_read_best_map()` 改用內建 `csv` 模組,不依賴 pandas | 2026-09 這台機器的 pandas 編譯 DLL(`tslibs\timezones`)被企業級應用程式控制原則封鎖(`ImportError: DLL load failed`),排除過 Controlled Folder Access(已確認關閉)跟檔案損毀(重灌後一樣的錯誤)兩種可能,判定是 IT 層級的封鎖,不是可以用程式碼修的問題。Random Search / Optuna TPE / Optuna CMA-ES 這三個 baseline 方法只需要讀 `results.csv` 抓最佳 mAP,不需要 pandas 的複雜功能,改用內建 `csv` 模組完全繞過。`src/summarize_log.py`(LLM 診斷組用的 Facts + Diagnostic Flags 計算,大量用到 pandas 的 rolling/diff)沒有比照修改,如果之後 Claude 系列模型的正式跑也遇到這個封鎖,需要聯繫實驗室 IT 處理,或評估是否值得把 `summarize_log.py` 也改寫成不依賴 pandas(工作量較大,暫不列入優先) |
| **`save_json` 的 numpy 序列化 + 非原子寫入 bug(已修正)** | 見 `src/utils.py` | 兩個獨立問題:(1) numpy/pandas 純量型別(`numpy.bool_` 等)未轉成原生型別就丟給 `json.dump()`,觸發 `TypeError`,根因在 `_is_train_loss_still_decreasing()` 少了 `bool()` 轉型,已修正並通用化(`save_json` 加上 `default=` callback 攔截所有 numpy 型別)。(2) 原本 `open(path, "w")` 會立刻清空檔案,若寫到一半當機(例如電腦被 Windows 自動更新強制重開機),會連之前已經存好的所有輪次進度一起銷毀,不只損失當輪。改成先寫暫存檔、成功後用 `os.replace()` 原子性取代,POSIX/Windows 都適用 |
| **LLM 輸出欄位完整記錄(已修正)** | `history` 每輪額外記錄 `llm_base_iteration`/`llm_base_rationale`/`llm_thinking` | 原本只存 `llm_reasoning` 跟提案的超參數,`base_iteration`/`base_rationale`(`prompts/program.md` JSON schema規定要輸出的欄位)跟 Qwen 的 `_thinking` chain-of-thought 被計算出來後沒有存進歷史紀錄,論文質化分析素材因此遺漏,已修正 `src/hpo_loop.py` 補齊 |
| **`summarize_log.py` 閾值常數與 novice baseline 改版脫節的 bug(2026-09-18 發現並修正)** | 已修正常數,已完成重跑(見下方新的一列) | 在 novice baseline 改成 5 個參數版之後,`prompts/program.md`(給 LLM 看的系統提示文字)跟 `results/d4_d5_thresholds.json`(推導用的資料)都已經更新成正確數字,但 `src/summarize_log.py` 裡實際計算 D1/D4/D5 的常數一直停留在更早的 SEARCH_SPACE 中點版(`NOISE_THRESHOLD=0.0119`/`D4=0.3155`/`D5=0.1991`),檔案自己的註解也寫著「先不要拿現在這幾個數字去跑正式實驗」,但當時 2026-09-17 晚上跑的 10 輪正式 Qwen 診斷實際上就是用這組錯誤常數跑的。用當時存下的原始 `results.csv` 重新計算後確認:10 輪裡有 5 輪(第4、5、6、7、9 輪)的 D1/D2/D5 至少一個旗標跟正確答案不同(D4 未受影響)。**已依決定完整重跑一次,見下方「Qwen3-14B 10 輪正式診斷重跑完成」一列,舊的污染版本已被取代** |
| **`experiments/run_baseline_3seed.py` 取代 `scripts/run_baseline_3seed.py`** | 已改用 `experiments/run_baseline_3seed.py`,`scripts/` 底下的舊版已改成停用提示,不會誤用 | 原本產生 3-seed 結果的腳本在 `scripts/run_baseline_3seed.py`,沒有中斷接續機制、seed 清單寫死讀 `config/settings.yaml`。擴充到 5-seed 時新寫了 `experiments/run_baseline_3seed.py`(支援中斷接續、`--seeds` 參數指定要跑哪些),已用它成功補跑 seed3、seed4。為避免兩份邏輯類似的腳本之後被搞混、誤用沒有接續機制的舊版覆蓋掉新結果,舊版已改寫成停用提示,統一用 `experiments/run_baseline_3seed.py` |
| **`claude-haiku-4.5` model id 修正(已完成)** | `src/llm_agent.py` 的 `CLAUDE_MODELS["claude-haiku-4.5"]` 從 `claude-haiku-4-5` 改成 `claude-haiku-4-5-20251001` | Haiku 4.5 早於「4.6 世代」開始用的無日期 model id 慣例,還是需要帶日期後綴的 dated snapshot 格式,原本寫的值會導致 API 呼叫失敗,已查證修正 |
| **新增 `--hinted` 追加對照組(2026-09-23)** | `prompts/program_hinted.md`(新檔案)、`experiments/run_full_diagnosis.py` 新增 `--hinted` 旗標,用法比照既有的 `--budget-aware` 模式 | 使用者觀察到 gemma4-12b 診斷組連續多輪(round1-7)觸發 D1/D2 卻始終無法真正跳出局部搜索(超參數只在已知最佳點附近做極小幅擾動,`mosaic` 全程未被探索過,round6 甚至因為防重複機制被迫在自己認定的最佳值 `box=8.0` 旁邊硬湊一個 `box=7.9` 交差),對照千問(qwen3-14b)在同樣多輪觸發 D1/D2 的情況下仍持續大幅探索(`mosaic` 有真的調動、`box` 試遍 10~15 的範圍)並在 round8 找到新高,差異明顯。與使用者討論後(GPT 也建議類似方向)決定追加一組「有提示版」對照,**不修改主線 `program.md`**(無提示比較設計維持不變),另外新增 `prompts/program_hinted.md`,加入兩段內容:(1) 診斷旗標→超參數對應的一般性經驗法則(D1/D2→降lr0/升weight_decay,D3→升weight_decay/augmentation,D4→升box,D5→視情況調hsv_v/mosaic/box),明確註明是參考先驗、非硬規則;(2) 針對 gemma4-12b 實際觀察到的「只敢微調、不敢跳脫」問題額外寫的探索提示,明確要求如果近幾輪都只是小幅微調且沒有實質進步,應該提出明顯不同的新配置、探索尚未測試過的維度。指令:`python experiments/run_full_diagnosis.py --model gemma4-12b --hinted`,結果存成 `results/progress_gemma4-12b_hinted.json`,不影響任何既有結果檔。**這組定位為探索性追加對照組,不進入主線 9 組比較表**,目的是驗證「給提示之後,診斷組表現能不能有大幅提升」,可作為 Discussion/Future Work 的補充證據;若論文要用這個結果支持「我們的方法優於對照組」的主張,需註明這已經不是原本「LLM 自主推理」的無提示設計,是額外加了研究者先驗知識的版本 |
| **`run_score_only_loop()` 實作完成** | 見 `src/hpo_loop.py`,對應 `experiments/run_score_only.py`(已加 `--model`/`--rounds` 參數,跟 `run_full_diagnosis.py` 用法一致) | 刻意重用 `run_hpo_loop()` 的 `_format_history`/`_format_best_reference`/`_validate_hyperparameters`/`_check_duplicate`/`_propose_with_retry`/`_load_resume_state`,確保兩組除了「有沒有 Facts + Diagnostic Flags」之外的機制(重試/重複偵測/resume/歷史最佳參考)完全對等,不會有其他混淆變因。`prompts/score_only.md` 原本缺「Run History Format 之後的實際格式」跟「Output Format」兩個區塊(只有標題沒有內容),已一併補完,格式對齊 `program.md` |
| **Qwen3-14B 10 輪正式診斷重跑完成(2026-09-18)** | `results/full_diagnosis_qwen3-14b.json`:新的 best_round=8,best_map5095=0.64892(取代舊的污染版 best_round=2, 0.65565) | 用修正後的最終 5-seed 閾值常數(`NOISE_THRESHOLD=0.02192252905118387`/`D4=0.326124`/`D5=0.226932`)完整重跑 10 輪,確認 10 輪的 `diagnostic_flags` 全部用同一套正確閾值算出(逐輪核對過)。注意這是**重新訓練**、不是只重算舊訓練結果的診斷旗標,所以最佳輪次跟分數本身都跟舊版不同(YOLO 訓練本身有隨機性,同一組超參數不會每次訓練出一模一樣的結果),這是預期中的正常現象,不是新的 bug。舊版(best_round=2, 0.65565)已完全被這份取代,論文正式引用一律用這份新結果 |
| **Optuna CMA-ES 決定排除在論文比較範圍外(2026-09-18)** | `results/baseline_optuna_cmaes.json` 保留在檔案系統中作為探索紀錄,但**不放進論文正式比較** | CMA-ES 本身機制在 10 輪預算內相對完整(暖機只需 1 輪),但為了不要讓比較範圍變得零散(一個傳統方法、兩個 Optuna 方法),且它的定位(示範傳統貝氏優化在低預算下的表現)已經由 Random Search 這個更單純的基準線涵蓋,故排除,不放進正式比較。見下方,Optuna TPE 的處理方式不同(不是排除,是併入 Random Search 當第二組獨立試驗) |
| **Optuna TPE 的 `n_startup_trials` 陷阱(2026-09-18 發現,查證到原始碼等級)** | `optuna.samplers.TPESampler()` 預設 `n_startup_trials=10`,剛好等於 `hpo.num_rounds=10` | 查證 Optuna 官方文件跟原始碼(`optuna/samplers/_tpe/sampler.py`)確認:`sample_independent()` 裡只要 `len(trials) < n_startup_trials` 就直接呼叫內建的 `RandomSampler` 生成該輪超參數,完全不會用到 TPE 的 KDE 機率模型。`num_rounds=10` 剛好等於 `n_startup_trials=10`,代表 10 輪從頭到尾都符合這個條件,TPE 演算法本身**一次都沒有被觸發過**,不是「跑得不準」,是完全沒跑到。也就是說 `results/baseline_optuna_tpe.json` 這 10 輪,本質上就是一次跟 Random Search 用同一套機制(均勻隨機取樣)、只是隨機種子不同的獨立試驗,不是一個真正的 Optuna TPE 結果 |
| **Random Search 基準線改用 Optuna TPE 資料,原始資料已刪除(2026-09-18,指導教授核准,取代前一版「兩組獨立試驗」的決定)** | 正式的「Random Search」基準線 = `results/baseline_random_search.json`(內容即 Optuna TPE 那組資料,本質上是純隨機取樣,見下方歷史紀錄)。`runs\detect\optuna_tpe_0~9` 已重新命名為 `runs\detect\random_search_0~9`,`results/baseline_random_search.json`/`progress_random_search.json` 內的 `results_csv`/`args_yaml` 路徑已同步改成指向新資料夾名稱,已驗證路徑跟資料夾內容對得起來(重新讀取 `random_search_0`/`random_search_9` 的 `results.csv`,數值跟 json 記錄一致)。原本真正用 `run_baselines.py --method random_search` 跑出來的結果、以及 `results/baseline_optuna_cmaes.json`,連同底下的 `runs\detect\random_search_*`(舊版)、`runs\detect\optuna_cmaes_*`、`runs\detect\val*` 資料夾,**都已經清除、不再保留於檔案系統** | 討論過程:一開始建議用 TPE 資料整組取代原本的 random_search 資料,我提醒這樣做即使技術理由成立(兩者機制相同,見下方歷史紀錄),實質上仍是在兩次隨機結果裡選對論文敘事更有利的一組、捨棄另一組,屬於選擇性資料呈現,建議改成兩組獨立試驗一起報告。使用者表示這是呈現方式的選擇、不違反學術倫理,並確認指導教授同意這樣處理,故依指示執行取代。原本先把舊 random_search 結果備份保留(`_original_v1` 檔名),但使用者後續明確要求連同 CMA-ES 資料一起從檔案系統清除做收尾,已執行(JSON 檔案直接刪除、`runs\detect` 底下的舊資料夾、CMA-ES 資料夾、以及過程中產生的 `val*` 驗證暫存資料夾都刪除)。**這個決策過程本身(原始資料存在過、以及最終被清除)只保留在這份 `docs/decisions.md` 內部紀錄裡,論文正文不會提及,論文引用「Random Search」時一律用 `results/baseline_random_search.json`(即 Optuna TPE 資料,folder 已改名為 random_search_*)** |
| **早期輪次效率比較(2026-09-18 第三次重新計算,Random Search 改回單一資料來源=Optuna TPE)** | 門檻定義不變:寬鬆 = baseline mean + 1×std = 0.630809、嚴謹 = baseline mean + 2×std = noise_threshold = 0.641771 | 比較 Random Search(=Optuna TPE 資料)跟 Qwen3-14B(新版)兩個方法:**Random Search** 寬鬆門檻第3輪(0.63983)達到、嚴謹門檻第4輪(0.64542)達到,10輪最終 best-so-far 0.64733,平均0.625446,3輪(第0、1、6輪,對應值0.57939/0.61646/0.60248)低於 baseline mean;**Qwen3-14B** 寬鬆門檻第2輪(0.63423)達到(比 Random Search 快)、嚴謹門檻第8輪(0.64892)達到(比 Random Search 慢很多),10輪最終0.64892(比 Random Search 高),平均0.633404(比 Random Search 高),0輪低於 baseline mean(比 Random Search 穩定)。**可以誠實寫的結論:Qwen 在「寬鬆門檻速度」「最終最佳值」「平均值」「從未低於 baseline」四項都優於這組 Random Search 資料,只有「嚴謹門檻速度」落後**,論文可以用這個角度呈現 LLM 優化的優勢,但要註明 Random Search 只用了一組樣本(本質上是隨機取樣的單次抽樣結果,原始的另一組真正 random_search 抽樣結果數值仍完整保留在上一列的歷史紀錄裡供內部對照,那組資料裡 Random Search 表現更好、最終達到0.66378,超過 Qwen 的0.64892,但檔案本身已依指示清除),這個限制建議誠實寫進論文,不要略過不提(是否採納由使用者/指導教授決定) |
| **Claude API 呼叫的兩個 bug(2026-09-18~19 發現並修正)** | 見 `src/llm_agent.py` `call_claude()` | (1) `text = response.content[0].text` 假設 `content` 第一個區塊一定是文字,但 Sonnet 5/Opus 5 的回應會夾帶一個 `type="thinking"` 的 `ThinkingBlock`(無 `.text` 屬性),且不保證排在第幾個位置,導致 `AttributeError`。改成明確找 `type=="text"` 的區塊。(2) 修好(1)之後,Sonnet 5 補做 round 0 提案時又遇到 `json.decoder.JSONDecodeError: Unterminated string`——原因是 `max_tokens=2048` 是 thinking + 最終文字輸出的總預算,常被 thinking 吃掉大半,導致要解析的 JSON 被截斷在字串中間。已拉高到 `max_tokens=8192`。這兩個 bug 只影響 Sonnet 5 / Opus 5(Haiku 4.5 的回應目前沒觀察到 thinking 區塊),Haiku 4.5 的全部結果不受影響。另外發現一個操作面的坑:overnight queue 腳本崩潰後自動接續下一步時,若下一個 python 行程在修正檔案寫回硬碟「之前」就已經啟動,該行程會用記憶體裡舊版程式碼繼續跑,不會因為硬碟檔案後來被改掉就換新——Opus 5 的第一次 round 0 崩潰就是這樣造成的,不代表修正沒生效,只代表已啟動的行程不會拿到新代碼 |
| **Sonnet 5 / Opus 5 round 0 訓練成果補救(2026-09-19)** | `scripts/recover_round0_claude-sonnet-5.py`、`scripts/recover_round0_claude-opus-5.py`(跟 Haiku 那支同邏輯,一次性腳本,已完成階段性任務) | 兩個模型都因為上述 API bug 在 round 0 訓練完成、LLM 提案呼叫失敗,`save_json()` 沒機會執行。訓練成果(`runs/detect/claude-sonnet-5_round0`、`claude-opus-5_round0`)都完整保留,用補救腳本重跑一次 LLM 提案(不重新訓練)補進 `results/progress_claude-*.json`,成功後兩個模型都能從 round 1 正常接續,沒有浪費任何已完成的訓練 |
| **完整實驗矩陣全部跑完(2026-09-19,`run_overnight_queue.ps1` 整夜自動接續,00:09~06:45,全部一次成功、無需重試)** | 9 組結果總表(10 輪皆完整,`map5095` 為 best-so-far): | 見下方獨立表格。核心發現:**Haiku 4.5 是唯一一個「完整診斷(Facts+Flags)」表現優於「純分數」的模型;Sonnet 5 跟 Opus 5 都是相反方向——兩者的 score-only 都明顯贏過各自的 full-diagnosis,Opus 5 的 full-diagnosis(0.64237)甚至輸給沒有任何 LLM 介入的 Random Search baseline(0.64733)**。這跟「診斷資訊越豐富、LLM 決策越好」的直覺假設相反,值得在論文 Discussion/Limitation 段落重點討論,可能的解釋方向包括:模型能力差異(Haiku 是非推理小模型,可能更依賴外部結構化診斷；Sonnet/Opus 有更強的內部推理能力,額外的 Facts+Flags 反而變成干擾資訊或誘發過度分析)、或診斷報告的資訊密度對不同規模模型的邊際效益不同。這個現象是否穩健(而非單次訓練隨機性),之後如果時間允許值得考慮補充 seed 穩健性驗證 |

**四個 LLM × 兩種條件 + Random Search baseline,共 9 組結果總表(best-so-far mAP50-95,由高到低排序):**

| 排名 | 方法 | best_map5095 | 最佳輪次 | round 0(共同起跑點) |
|---|---|---|---|---|
| 1 | Claude Haiku 4.5 — full-diagnosis | 0.66700 | round 7 | 0.62692 |
| 2 | Claude Opus 5 — score-only | 0.66298 | round 4 | 0.62692 |
| 3 | Claude Sonnet 5 — score-only | 0.66178 | round 7 | 0.62692 |
| 4 | Claude Haiku 4.5 — score-only | 0.65342 | round 9 | 0.62692 |
| 5 | Claude Sonnet 5 — full-diagnosis | 0.65166 | round 7 | 0.62692 |
| 6 | Qwen3-14B — score-only | 0.65155 | round 3 | 0.62692 |
| 7 | Qwen3-14B — full-diagnosis | 0.64892 | round 8 | 0.62692 |
| 8 | Random Search baseline(=Optuna TPE 資料,見上方) | 0.64733 | trial 7 | 0.57939 |
| 9 | Claude Opus 5 — full-diagnosis | 0.64237 | round 5 | 0.62692 |

(四個 LLM 方法的 round 0 全部相同,因為都是從同一組 novice baseline 起始超參數開始;Random Search 每一輪都是獨立隨機取樣,round 0 不受起始超參數約束,數值明顯較低。)

⚠️ 重要:score-only 對照組、四個 LLM 方法、Random Search 基準線,全部都要在**同一台機器、同一組 batch size、同一組起始超參數**下跑,才能公平比較。這個前提已滿足(見上方新手起始點定案)。

| **`runs/detect/` 資料夾撞名事故(2026-09-19)** | 已修正,見 `src/hpo_loop.py` 模組開頭第6點、`experiments/run_full_diagnosis.py`/`run_score_only.py` 的 `folder_suffix` 傳遞 | 原本計畫用 `--rounds 10`(數值刻意跟預設一樣,只是想拿到不同檔名)跑 Opus/Sonnet 的第二次獨立複製(replicate 2),驗證「完整診斷 vs 純分數」的反轉現象穩不穩健。`results/` 底下的 json 檔名有靠 `_r10` 後綴分開,但 `runs/detect/<run_name>` 資料夾命名沒有用到這個後綴,`train_run()` 發現資料夾已存在會整個刪掉重練,實際執行到 Opus 完整診斷 round 0 時,**已經把第一次複製的 `runs/detect/claude-opus-5_round0` 原始訓練資料夾(權重、confusion matrix 等)蓋掉了**。使用者當場發現並手動中斷(Ctrl+C),避免了後續 round1-9 跟 Sonnet 兩組也被蓋掉。**這次事故不影響任何論文分析用的資料**:`results/full_diagnosis_claude-opus-5.json`、`results/progress_claude-opus-5.json` 這些已經萃取好的結果檔(所有分析都是根據這些檔案做的)完全沒被動到,只有原始訓練產物(可以重新訓練生成,但無法還原第一次跑的那份)遺失。已修正 `run_name` 加上跟 json 檔名一樣的後綴,之後任何重跑都不會再撞名。中斷前留下的 `results/progress_claude-opus-5_r10.json`(只有 round 0 一筆記錄)跟被蓋掉後重練的 `runs/detect/claude-opus-5_round0` 資料夾(內容已對不上論文分析用的 json)兩個殘留,已於 2026-09-19 收尾階段由使用者手動刪除(見下方「一次性腳本/殘留檔案清理」一列) |
| **budget-aware 條件實驗完成,測試「LLM 不知道優化預算」假說(2026-09-19)** | 見 `prompts/program_budget_aware.md`、`src/hpo_loop.py` 的 `include_budget_info` 參數。最終結果:`results/full_diagnosis_claude-opus-5_budgetaware.json` best_round=9、best_map5095=0.63203;`results/full_diagnosis_claude-sonnet-5_budgetaware.json` best_round=7、best_map5095=0.6664 | 使用者觀察 Opus 5 完整診斷組(第一次跑)的推理文字後提出假說:Opus 完全不知道這個優化總共只有 10 輪預算(系統提示跟每輪報告都從沒提過),所以採取「像做嚴謹科學實驗一樣,一次只調一個變因、還規劃好幾輪以後要測什麼」的長線策略,而不是在預算有限時改採更大膽的多變因調整。查證屬實:`prompts/program.md`/`score_only.md`、`report_text` 組成邏輯,確實從未告訴 LLM 總輪數/剩餘輪數。新增獨立的 budget-aware 條件測試這個假說,只加在完整診斷組(不動純分數組),只給 LLM 客觀事實(目前第幾輪/總共幾輪/還剩幾輪),不指定該怎麼反應。2026-09-19 09:05 開始跑 Opus,10:33 完成;接著跑 Sonnet,11:55 完成。**結果兩個模型方向不一致**:Opus budget-aware(0.63203)反而比第一次不知道預算的版本(0.64237)還差,假說在 Opus 身上沒有得到支持;Sonnet budget-aware(0.6664)明顯優於第一次的 0.65166,而且 round8(倒數第二輪)的推理文字明確寫出「This is the final round (10/10), so I am prioritizing consolidation... we have no further rounds to explore a new hypothesis if it fails」,顯示 Sonnet 確實因為知道剩餘輪數而在後段主動收斂到保守路徑,假說在 Sonnet 身上得到具體驗證(數字+推理文字雙重證據)。由於兩模型效果不一致、且已與「論文正式資料集拍板」那筆決定確認過此組不列入主要 9 組比較表,這裡的發現定位為 Discussion/Future Work 的補充證據 |
| **論文正式資料集拍板:採用第一次(原始、不知道預算)實驗結果(2026-09-19)** | 上方「9 組結果總表」正式定案為論文 Results 章節的主要資料來源;budget-aware 那組定位為探索性補充材料,不進入主要結果比較 | 使用者在 budget-aware 實驗(當時進度:Opus 9/10 輪、Sonnet 尚未開始)還沒跑完的情況下,決定不等它,直接以第一次(不知道總輪數預算)的 9 組結果動筆寫論文。理由:budget-aware 是為了驗證「Opus 表現差是否因為不知道預算」這個事後假說才追加的探索性實驗,不是要重跑或取代原本的實驗設計;不論這個假說成不成立,都不影響第一次結果本身的正當性——第一次的條件(LLM 不被告知輪數預算)才是四個模型 × 兩種診斷條件這組對照實驗最初的共同設計,budget-aware 版本改變了 Opus/Sonnet 全診斷組拿到的資訊量,不再是跟其他七組完全公平對等的條件,不適合直接混進同一張比較表。budget-aware 已於 2026-09-19 全部跑完(見上一列),最終結果只作為 Discussion/Future Work 段落的補充證據,不列入正式的 9 組比較總表,也不會改動已經寫定的排名結論(見上方「完整實驗矩陣全部跑完」那一列) |
| **一次性腳本/殘留檔案清理完成(2026-09-19)** | 已刪除:`run_overnight_queue.ps1`、`run_rep2_queue.ps1`、`run_budgetaware_queue.ps1`、`scripts/recover_round0_claude-haiku-4.5.py`、`scripts/recover_round0_claude-sonnet-5.py`、`scripts/recover_round0_claude-opus-5.py`、`results/progress_claude-opus-5_r10.json`、`runs/detect/claude-opus-5_round0`(撞名事故蓋掉後重練的資料夾)。已用 `device_list_dir` 實機確認上述項目確實不存在,其餘結果檔案(`results/` 底下 9 組主線 + 2 組 budget-aware,共 11 組 full_diagnosis/progress json)跟訓練資料夾均完好未受影響 | 三支 log 檔(`overnight_queue_log.txt`、`budgetaware_queue_log.txt`、`rep2_queue_log.txt`)保留未刪,作為時間軸紀錄跟 decisions.md 對照用,體積很小,不影響收尾 |

## 待確定

| 項目 | 現況 | 待辦 |
|---|---|---|
| **論文寫作正式啟動** | 主要資料集已拍板(第一次 9 組結果),budget-aware 補充實驗也已跑完,一次性腳本/殘留檔案已清理完畢,`docs/decisions.md` 記錄齊全,可以開始動筆 | 待使用者確認先做哪一項(可並行):(a) 補齊 Sonnet/Opus 的 D1-D5 觸發模式橫向比較(目前只有 Haiku 做過,見下方);(b) 製作最終比較圖表;(c) 直接開始寫 Results/Discussion 文字段落。由使用者決定優先順序 |
| 「診斷資訊越多不一定越好」這個反直覺發現的穩健性 | 目前只有單次訓練(每組合各跑1次×10輪),Sonnet/Opus 的 full-diagnosis 落後 score-only 有可能是訓練隨機性造成,不一定是系統性效應 | 時間允許的話,考慮針對 Sonnet 5 / Opus 5 的 full-diagnosis vs score-only 這兩組對照補跑不同 seed 驗證穩健性,或至少在論文 Limitation 段落誠實註明這點,不要把單次結果講成定論 |
| 四個 LLM 方法的 D1-D5 觸發模式橫向比較 | Haiku 的診斷組已確認幾乎每輪(round 1-9)都觸發 D1+D2,round 0 觸發 D4,D3/D5 全程未觸發;Sonnet/Opus 的診斷組觸發模式尚未整理 | 把 Sonnet/Opus 的 full-diagnosis 10 輪 diagnostic_flags 跟 Haiku 一樣整理出來,看四個模型面對同樣的診斷旗標組合,實際超參數調整方向有沒有系統性差異(已有 Haiku 兩個範例輪次的 llm_reasoning 質化分析,可以比照做 Sonnet/Opus) |
| 最終比較圖表/論文結果章節 | 9 組結果總表已整理(見上方),尚未做成論文用的圖(收斂曲線疊圖、最終結果長條圖等) | 待使用者確認需要哪些圖表類型後製作 |
| gemma4-12b 有提示版(`--hinted`)實測結果 | 已完成程式碼與 prompt 設計(見上方「已確定」表),尚未執行。原始 9 工作佇列已跑到第 8 個(gemma4-31b 診斷組 round1)時撞到 Windows 分頁檔不足(`OSError: [WinError 1455]`)整個卡死,`scripts/run_remaining_queue.py` 已於 2026-09-23 改版,縮減為只剩 ministral-3-14b / llama3.1-8b 的 score-only 兩個工作,gemma4-31b(診斷組+score-only)先暫緩不跑 | 待辦順序:(1) 使用者手動增加 Windows 分頁檔(C: 槽,起始 16384MB/上限 32768MB)並重開機;(2) 重開機後重跑 `python scripts\run_remaining_queue.py`(現在只會跑 ministral-3-14b、llama3.1-8b 的 score-only,約 20-30 分鐘可跑完);(3) 確認這兩個 score-only 成功後,再手動執行 `python experiments/run_full_diagnosis.py --model gemma4-12b --hinted`,比較有提示 vs 無提示版本的最終 best_map5095 差距,並檢查有提示版是否真的探索了 `mosaic` 等先前完全沒碰過的維度;(4) 分頁檔問題確認解決後,視情況再手動補跑 gemma4-31b(診斷組+score-only)跟 ministral-3-14b/llama3.1-8b 的診斷組(llama3.1-8b 診斷組已有 round4/11 的部分進度,resume 會自動接續) |
| 雪糕之前「用不了」的原因 | 未確認,但已排除硬體規格差異(GPU同級) | 可能仍是絕對路徑寫死或環境安裝問題(新專案已改相對路徑,理論上已解決,但建議直接問他當初的錯誤訊息以確認),不影響其他項目,可平行處理 |
